# frontend (C 端 H5) 环境变量样例
# 复制成 .env.local 来覆盖默认值

# API 模式：
#   mock = 100% 本地 mock（默认，离线演示用）
#   real = 高价值接口（登录 / AI 出图 / 偏好上报）走真后端，其余仍为 mock
VITE_API_MODE=mock

# 真后端地址（vite dev proxy 用，默认 http://localhost:8200）
VITE_API_TARGET=http://localhost:8200
